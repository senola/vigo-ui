import Bubby from './bubbly';
import Matrix from './matrix';
import PaperPlane from './paper-plane';
import StarField from './star-field';

const Background = {
    Bubby,
    Matrix,
    PaperPlane,
    StarField
};

export default Background;
